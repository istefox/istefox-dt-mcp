# v0.3.0 — community announcement draft

> Style guide: ~200 words max, single-level bullets, `**bold**` instead of `##` headers, double blank line between sections. Paste in Reddit's Markdown Editor (not Fancy). Source: memory `feedback_community_post_style.md`.

**Targets** (in order of relevance):
1. **r/devonthink** — primary audience, already posted 0.2.0 there on 2026-05-03.
2. **DEVONtechnologies forum** (`discourse.devontechnologies.com`) — Tips & Tricks / Automation subforum.
3. **Hacker News (Show HN)** — skip for 0.3.0 (minor release). Save for 0.4.0 if HTTP transport lands.
4. **awesome-mcp-servers** — verify listing description is current; PR if obsolete.

---

## Post body

```markdown
**istefox-dt-mcp 0.3.0 — drift detection on `bulk_apply` undo**

https://github.com/istefox/istefox-dt-mcp

Quick follow-up to 0.2.0 from last week.


**What's new**

- Per-op drift detection in `bulk_apply` undo, 3-state classifier (no_drift / already_reverted / hostile_drift). Same logic 0.2.0 added to `file_document` undo, now extended to batch operations.
- If a record was externally modified after the bulk apply, the undo skips it and surfaces the diff (`drift_details`). Add `--force` if you want to overwrite anyway.
- If you already manually reverted a record before running undo, it's detected and skipped silently — no spurious "drift" warning.
- One-shot release pipeline: tag push now auto-publishes to the MCP Registry (was a manual second step before).


**Why it matters**

Bulk operations on tagged groups of records (re-tag 50 PDFs, move "research/2025" to archive, etc.) are easier to roll back safely. The undo no longer assumes nothing changed since the apply — it checks per record.

222 unit + contract tests, mypy clean.


**Roadmap**: HTTP transport + OAuth (multi-device), `create_smart_rule` (blocked on a DT4 SDK gap), RAG benchmark cross-corpus.

Feedback welcome.
```

---

## Notes

- Word count: ~190 words (target ≤200).
- Title: deliberately specific — "drift detection on `bulk_apply` undo" tells r/devonthink readers exactly what changed without reading the body. Avoids generic "v0.3.0 released" titles that get scrolled past.
- Link is on line 3, not buried mid-post — Reddit users on mobile see it without scrolling.
- "Feedback welcome." at the end is intentional — no aggressive CTA, no "please upvote", no "let me know what you think". Aligns with the asciutto style validated for 0.2.0.
